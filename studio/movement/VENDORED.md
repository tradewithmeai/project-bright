# Vendored copy — do not edit here

Canonical source: `skelator/movement/` (research repo).
Synced 2026-07-27 21:14 by `sync_movement_db.py`.

| file | version |
|---|---|
| `walk_spatiotemporal.json` | 0.5 |

Edits belong in the canonical copy. Re-run the sync to update this one; the loaded version is stamped into every `gait_root.json` and render, so a stale vendored sheet is visible in the output rather than in the behaviour.
