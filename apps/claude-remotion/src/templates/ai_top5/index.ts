export { AiTop5Composition } from "./AiTop5Composition";
// TOTAL_FRAMES comes from grid.ts: the episode's length is derived from its musical structure and
// the day's measured voiceover clips, so it is a property of the bulletin rather than a constant.
export { TOTAL_FRAMES, describeLayout } from "./grid";
